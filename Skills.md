# Skills.md — Deep-Value Quant Guardrail Dashboard
## 투자 데이터 분석 규칙 정의서

**작성자:** 고려대학교 통계학과 송호빈  
**버전:** v1.0  
**목적:** 본 문서는 어떤 투자 데이터 구조가 입력되더라도 동일한 분석 파이프라인이 자동으로 작동하도록 분석 규칙, 시각화 기준, 인사이트 생성 규칙을 명문화한다. LLM(Claude 등)이 본 문서를 컨텍스트로 읽고 대시보드 코드를 자동 생성하는 **프롬프트 엔지니어링 청사진(Blueprint)** 으로 기능한다.

---

## 목차

1. [데이터 분류 및 전처리 규칙](#1-데이터-분류-및-전처리-규칙)
2. [Data Mapping Rule (범용성 핵심)](#2-data-mapping-rule)
3. [가치 평가 지표 계산 규칙](#3-가치-평가-지표-계산-규칙)
4. [거시경제 민감도 분석 규칙](#4-거시경제-민감도-분석-규칙)
5. [다중공선성 진단 규칙](#5-다중공선성-진단-규칙)
6. [통합 Guardrail Score 산출 규칙](#6-통합-guardrail-score-산출-규칙)
7. [시각화 선택 기준](#7-시각화-선택-기준)
8. [인사이트 자동 생성 규칙](#8-인사이트-자동-생성-규칙)
9. [범용성 적용 예시 (3가지 데이터 구조)](#9-범용성-적용-예시)

---

## 1. 데이터 분류 및 전처리 규칙

### 1-1. 데이터 타입 자동 분류

입력 데이터를 아래 3가지 타입으로 자동 판별한다.

| 타입 | 판별 기준 (컬럼명 키워드 포함 여부) | 시간 단위 |
|------|--------------------------------------|-----------|
| **재무 데이터 (정적)** | 자산, 부채, 자본, 매출, 영업이익, asset, liability, equity, revenue | 분기 (Quarter) |
| **시장 데이터 (동적)** | 종가, 시가, 고가, 저가, 거래량, close, open, high, low, volume, price | 일별 (Daily) |
| **거시 데이터 (외부)** | 금리, 환율, 물가, CPI, rate, exchange, inflation, fed, krw, usd | 월별 (Monthly) |

판별 불가 시: 사용자에게 수동 선택 UI 제공

### 1-2. 전처리 규칙

**결측값 처리:**
- 재무 데이터: 직전 분기값으로 대체 (Forward Fill). 2분기 이상 연속 결측 시 해당 종목 분석 제외 및 경고 표시
- 시장 데이터: 선형 보간(Linear Interpolation). 5거래일 이상 연속 결측 시 거래정지 플래그 부여
- 거시 데이터: 월별 평균값으로 리샘플링 후 시장 데이터와 병합

**이상치 처리:**
- 시장 데이터: 로그 수익률 기준 ±3σ 초과값은 이상치로 분류, 분석에서 제외하되 차트에 별도 마커로 표시
- 재무 데이터: 전분기 대비 500% 이상 변동 시 경고 플래그 부여 (분식회계 가능성)

**단위 통일:**
- 재무 데이터: 억원(KRW 100M) 단위 통일
- 외화 자산 포함 시: 입력일 기준 USD/KRW 환율 자동 적용하여 원화 환산
- 주가: 원(KRW) 단위 유지, 수익률은 로그 수익률(log return) 사용

---

## 2. Data Mapping Rule

**핵심 원칙:** 컬럼명이 달라도 의미(Entity)가 같으면 동일하게 처리한다.

### 2-1. Entity 매핑 테이블

| 표준 엔티티명 | 허용 컬럼명 변형 (예시) |
|---------------|-------------------------|
| `current_assets` | 유동자산, CurrentAsset, current_asset, 流動資産, curr_assets |
| `total_liabilities` | 총부채, TotalLiability, total_liability, 負債合計, liabilities |
| `market_cap` | 시가총액, MarketCap, market_cap, mktcap, 時価総額 |
| `close_price` | 종가, Close, close_price, adj_close, 종가(원) |
| `interest_rate` | 금리, InterestRate, interest_rate, base_rate, fed_rate, 기준금리 |
| `exchange_rate` | 환율, ExchangeRate, usd_krw, USD/KRW, fx_rate |

### 2-2. 매핑 실패 처리

1. 자동 매핑 실패 시 → 유사도 기반 후보 3개 제안 (예: "total_assets와 매핑하시겠습니까?")
2. 사용자가 거부 시 → 수동 드래그-앤-드롭 매핑 UI 제공
3. 필수 엔티티(`current_assets`, `total_liabilities`, `market_cap`) 중 하나라도 매핑 불가 시 → NCAV 계산 불가 경고 후 가능한 지표만 부분 분석 실행

---

## 3. 가치 평가 지표 계산 규칙

### 3-1. NCAV (Net Current Asset Value) 계산

```
NCAV = 유동자산(current_assets) - 총부채(total_liabilities)
안전마진 비율 = NCAV / 시가총액(market_cap)
```

### 3-2. NCAV 등급 분류 기준

| 등급 | 조건 | 색상 | 통계적·학문적 근거 |
|------|------|------|-------------------|
| **Strong Buy** | 시가총액 < NCAV × 1.0 | 🟢 진녹색 | Benjamin Graham(1934) 원전 기준. 청산가치 이하 매수로 이론적 손실 하한이 존재 |
| **Deep Value** | 시가총액 < NCAV × 1.5 | 🟢 연녹색 | Oppenheimer(1986) 실증 연구: NCAV 1.5배 이하 포트폴리오 연평균 초과수익률 29.4% 확인 (NYSE 1970~1983) |
| **관심 종목** | 시가총액 < NCAV × 2.0 | 🟡 노랑 | 국내 코스피 기준 PBR 1배 이하 종목군과 80% 이상 겹치는 구간 (한국거래소 통계 기준) |
| **분석 제외** | 그 외 | 🔴 빨강 | 가치투자 기준 안전마진 미확보. 분석 불가가 아닌 "가치투자 대상 아님" 으로 표시 |

### 3-3. PBR 보조 지표 규칙

```
PBR = 시가총액 / 자기자본(stockholders_equity)
```

| PBR 구간 | 플래그 | 의미 |
|----------|--------|------|
| PBR < 0.3 | 🚨 구조적 저평가 경고 | 시장이 청산가치도 인정 안 하는 상황. NCAV 분석 신뢰도 상승 |
| 0.3 ≤ PBR < 0.5 | ⚠️ 안전마진 확인 구간 | NCAV와 병행 분석 권장 |
| PBR ≥ 1.0 | ℹ️ 가중치 감소 | NCAV 분석과 병행 시 가치점수 가중치 50% 감소 적용 |

### 3-4. EV/EBITDA 보조 지표 (데이터 가용 시)

```
EV = 시가총액 + 순부채(총부채 - 현금및현금성자산)
EV/EBITDA < 5: 저평가 신호 (보조 지표로만 활용)
```

---

## 4. 거시경제 민감도 분석 규칙

### 4-1. 상관계수 산출 방식

- **변수:** 월별 주가 로그수익률 vs 거시지표 월별 변화율
- **방법:** Pearson 상관계수 (r)
- **최소 표본:** 36개월 (통계적 안정성 확보를 위한 최소 요건. 표본이 36개월 미만이면 "표본 부족" 경고)
- **유의성 검정:** t-검정 적용
  - `t = r × √(n-2) / √(1-r²)`
  - p-value > 0.05: "통계적으로 유의하지 않음" 경고 표시 (상관관계 수치는 표시하되 회색 처리)
  - p-value ≤ 0.05: 정상 표시

### 4-2. 임계값 설정 근거 (왜 |r| = 0.7인가)

Cohen(1988)의 효과크기(Effect Size) 분류를 금융 데이터에 적용:

| |r| 구간 | Cohen 분류 | 금융적 의미 |
|-----------|------------|-------------|
| < 0.3 | 약한 효과 | 노이즈 수준. 분석 실익 없음 |
| 0.3 ~ 0.5 | 중간 효과 | 참고 수준 |
| 0.5 ~ 0.7 | 강한 효과 | 유의미한 관계 |
| ≥ 0.7 | **매우 강한 효과** | **r² ≥ 0.49: 거시변수가 주가 분산의 49% 이상 설명. 투자 리스크 관리 임계값으로 설정** |

> **핵심 근거:** |r| ≥ 0.7은 결정계수 R² ≥ 0.49를 의미하며, 이는 해당 거시변수가 주가 움직임의 주도적 설명변수가 됨을 나타낸다. 이 수준에서는 종목 자체 분석보다 거시 국면 판단이 선행되어야 하므로 리스크 관리 임계값으로 설정한다.

### 4-3. 금리 민감도 등급

| 등급 | 조건 | 색상 | 투자 시사점 |
|------|------|------|------------|
| **금리 고민감** | r ≤ -0.7 (p ≤ 0.05) | 🔴 빨강 | 금리 인상 국면 매수 자제. 헷징 전략 필요 |
| **금리 중민감** | -0.7 < r ≤ -0.3 (p ≤ 0.05) | 🟡 노랑 | 금리 방향성 확인 후 진입 권장 |
| **금리 저민감** | r > -0.3 또는 p > 0.05 | 🟢 초록 | 금리와 독립적. 종목 자체 분석 우선 |

### 4-4. 환율 민감도 등급

동일 기준 적용. 단, 수출 비중이 높은 종목(매출의 50% 이상이 해외)은 환율 민감도를 금리 민감도보다 **우선 표시**한다.

| 등급 | 조건 | 투자 시사점 |
|------|------|------------|
| **환율 고민감** | r ≥ 0.7 (수출주: 환율 상승 수혜) 또는 r ≤ -0.7 (내수주) | 환율 방향성이 주가 결정적 요인 |
| **환율 중민감** | 0.3 ≤ \|r\| < 0.7 | 환율 참고 후 진입 |
| **환율 저민감** | \|r\| < 0.3 | 환율 영향 미미 |

---

## 5. 다중공선성 진단 규칙

### 5-1. 목적

거시지표(금리, 환율, CPI 등)는 서로 상관관계를 가질 수 있다. 다중공선성이 존재하면 개별 지표의 회귀계수 추정이 불안정해지고 해석이 왜곡된다. 이를 사전 탐지하고 처리하는 규칙을 명시한다.

### 5-2. 진단 절차

**Step 1: 거시지표 간 상관계수 확인**
- 분석에 사용할 거시지표 쌍(예: 금리 vs 환율, 금리 vs CPI)의 Pearson r 산출
- |r| ≥ 0.8이면 다중공선성 의심 → Step 2로 진행

**Step 2: VIF(분산팽창요인) 산출**
```
VIF_j = 1 / (1 - R²_j)
(R²_j: j번째 지표를 다른 지표들로 회귀했을 때의 결정계수)
```

**Step 3: VIF 기준 처리**
| VIF 값 | 판정 | 처리 방법 |
|--------|------|-----------|
| VIF < 5 | 다중공선성 없음 | 모든 지표 분석에 포함 |
| 5 ≤ VIF < 10 | 중간 수준 | 경고 표시 후 분석 계속 |
| VIF ≥ 10 | **심각한 다중공선성** | **단순회귀 R²가 더 높은 지표 하나만 최종 선택, 나머지 제외** |

> **VIF ≥ 10 기준 근거:** Hair et al.(2010) 다변량 분석 교재의 표준 임계값. VIF = 10은 해당 변수의 분산 중 90%(= 1 - 1/10)가 다른 변수들로 설명됨을 의미하며, 이 수준에서는 독립변수로서의 정보가치가 사실상 소멸한다.

### 5-3. 다중공선성 진단 결과 표시

- 진단 결과를 대시보드 우측 패널에 항상 표시
- VIF ≥ 10 발생 시: "⚠️ [지표명]이 분석에서 제외되었습니다 (VIF=[값]). [대체지표명]을 기준으로 분석합니다." 자동 알림

---

## 6. 통합 Guardrail Score 산출 규칙

### 6-1. 산출 공식

```
Guardrail Score = (가치점수 × 0.5) + (거시안전점수 × 0.3) + (재무건전성점수 × 0.2)
```

### 6-2. 각 점수 산출 기준

**가치점수 (0~100):**
| 조건 | 점수 |
|------|------|
| Strong Buy | 100 |
| Deep Value | 75 |
| 관심 종목 | 50 |
| 분석 제외 | 0 |

> PBR ≥ 1.0인 경우 가치점수 × 0.5 적용 (신뢰도 감소 패널티)

**거시안전점수 (0~100):**
| 조건 | 점수 |
|------|------|
| 금리·환율 모두 저민감 | 100 |
| 하나가 중민감 | 70 |
| 하나가 고민감 | 40 |
| 둘 다 고민감 | 10 |

> 통계적 비유의(p > 0.05) 지표는 저민감으로 처리하되, "데이터 불충분" 주석 추가

**재무건전성점수 (0~100):**
```
부채비율 = 총부채 / 자기자본 × 100
유동비율 = 유동자산 / 유동부채 × 100
```
| 조건 | 점수 |
|------|------|
| 부채비율 < 100% AND 유동비율 > 150% | 100 |
| 둘 중 하나만 충족 | 60 |
| 둘 다 미충족 | 20 |

### 6-3. 가중치 설정 근거

- **가치(50%):** 가치투자 원칙상 내재가치 확인이 최우선
- **거시안전(30%):** 매크로 환경이 진입 타이밍을 결정하는 핵심 변수
- **재무건전성(20%):** 하방 리스크 관리 목적. 부도 리스크 최소화

### 6-4. Guardrail Score 해석 기준

| Score 구간 | 등급 | 색상 | 투자 판단 |
|-----------|------|------|----------|
| 80 ~ 100 | **Strong** | 🟢 | 가치·거시·재무 모두 양호. 적극 검토 |
| 60 ~ 79 | **Moderate** | 🟡 | 일부 리스크 존재. 조건부 검토 |
| 40 ~ 59 | **Caution** | 🟠 | 리스크 우세. 관망 권장 |
| 0 ~ 39 | **Avoid** | 🔴 | 복수 리스크 존재. 진입 비권장 |

---

## 7. 시각화 선택 기준

**원칙:** 데이터의 성격과 분석 목적에 따라 차트 타입을 자동 결정한다. 사용자 선택이 아닌 규칙 기반 자동 선택이 원칙이다.

| 분석 내용 | 차트 타입 | 선택 근거 |
|-----------|-----------|-----------|
| 주가 vs 거시지표 상관관계 | 산점도(Scatter) + OLS 회귀선 + 95% 신뢰구간 | 두 연속변수의 선형 관계, 이상치, 불확실성 동시 파악 |
| 종목별 가치 등급 비교 (3종목 이상) | 히트맵 (종목 × 지표 매트릭스) | 다차원 정보를 단일 뷰로 압축, 색상으로 직관적 파악 |
| 주가·지표 시계열 | 이중 Y축 라인차트 | 스케일이 다른 두 시계열 동시 시각화 |
| Guardrail Score 전체 분포 | 버블차트 (X:가치점수, Y:거시안전점수, Size:시총) | 3차원 정보(가치·거시·규모)를 2D 평면에 표현 |
| 포트폴리오 비중 | 트리맵 | 계층적 비중 구조를 면적으로 직관적 표현 |
| 지표 2개 이하 비교 | 바차트 | 단순 비교에 가장 적합한 기본형 |
| 지표 3개 이상 종합 비교 | 레이더차트 | 다차원 속성 균형 파악 |
| 상관계수 행렬 | 코릴레이션 히트맵 | 변수 간 관계 구조 전체 파악 (다중공선성 진단에 활용) |

**색상 코딩 일관성 규칙:**
- 긍정/안전: `#2ECC71` (초록)
- 주의: `#F39C12` (주황)
- 위험/부정: `#E74C3C` (빨강)
- 중립/정보: `#3498DB` (파랑)
- 모든 차트에서 동일한 색상 체계 유지

---

## 8. 인사이트 자동 생성 규칙

**원칙:** 분석 결과를 산출값과 템플릿을 결합하여 자연어 문장으로 자동 생성한다.

### 8-1. 가치 평가 인사이트 템플릿

```
[종목명]의 현재 시가총액은 [시가총액]억원으로, NCAV [NCAV값]억원 대비 [배수]배 수준입니다.
이는 [등급명]에 해당하며, [등급별 코멘트].
보조지표: PBR [PBR값]배로 [PBR 해석].
```

등급별 코멘트:
- Strong Buy: "벤저민 그레이엄의 청산가치 기준을 충족합니다. 이론적 하방 리스크가 제한적입니다."
- Deep Value: "Oppenheimer(1986) 기준 초과수익 구간에 진입하였습니다."
- 관심 종목: "안전마진이 존재하나, 추가 재무 분석이 권장됩니다."
- 분석 제외: "현재 가격이 청산가치를 상회합니다. 가치투자 기준에서는 안전마진이 미확보된 상태입니다."

### 8-2. 거시 민감도 인사이트 템플릿

```
[종목명]과 기준금리의 Pearson 상관계수는 [r값] (p=[p값], n=[표본수]개월)으로,
[유의/비유의] 수준의 [등급명] 관계를 보입니다.
현재 금리 [상승/하락/동결] 국면에서 [리스크 코멘트].
```

### 8-3. 이상치 및 경고 인사이트

- Guardrail Score가 전체 종목 상위 5%: "🏆 해당 종목은 분석 대상 중 최상위 Guardrail Score를 기록하였습니다."
- Guardrail Score가 전체 종목 하위 5%: "⚠️ 복수의 리스크 요인이 감지되었습니다. 진입 전 심층 분석을 권장합니다."
- VIF ≥ 10 발생: "📊 다중공선성 감지: [제외된 지표]가 분석에서 자동 제외되었습니다."
- 표본 36개월 미만: "📉 분석 기간이 36개월 미만으로, 상관계수의 통계적 신뢰도가 제한됩니다."

---

## 9. 범용성 적용 예시

동일한 Skills.md 규칙이 서로 다른 데이터 구조에 적용되는 것을 3가지 케이스로 검증한다.

### Case 1: 개별 종목 재무 데이터

**입력:** 분기별 재무제표 CSV (현대제철)
```
컬럼: 분기, 유동자산, 총부채, 자기자본, 시가총액, 종가, 유동부채
```
**적용 규칙:**
1. Data Mapping: '유동자산' → `current_assets` 자동 매핑
2. NCAV 계산 → Strong Buy 분류 (시총 < NCAV × 1.0)
3. PBR 0.2 → 구조적 저평가 경고 플래그
4. Guardrail Score 산출

**출력:** 히트맵 + "현대제철은 NCAV 대비 시총이 0.6배 수준으로 Strong Buy 구간에 위치합니다."

---

### Case 2: ETF 데이터

**입력:** ETF 일별 NAV, 거래량, 추적오차 CSV (KODEX 200)
```
컬럼: date, nav, volume, tracking_error, benchmark_return
```
**적용 규칙:**
1. Data Mapping: 재무 엔티티 매핑 불가 → "ETF 데이터 감지: NCAV 분석 불가, 효율성 분석으로 전환" 자동 알림
2. 가치점수 대신 추적오차 역수 기반 효율성 점수로 자동 대체
3. 거시 민감도 분석 동일 적용 (주가→NAV 수익률 사용)

**출력:** 시계열 라인차트 + "KODEX 200은 기준금리와 r=-0.61 (p=0.003)의 금리 중민감 관계를 보입니다."

---

### Case 3: 포트폴리오 혼합 데이터

**입력:** 5개 종목 보유 비중, 매수가, 현재가 CSV
```
컬럼: ticker, name, weight, buy_price, current_price, sector
```
**적용 규칙:**
1. 종목별 Guardrail Score 개별 산출
2. 포트폴리오 가중 평균 Score = Σ(종목 Score × 보유 비중)
3. Score 하위 종목 → 리밸런싱 대상 자동 식별

**출력:** 버블차트 + 트리맵 + "포트폴리오 내 [종목명]이 Guardrail Score [X]점으로 Avoid 구간 진입. 비중 축소 검토 권장."

---

## 부록: LLM 코드 생성을 위한 Blueprint 지침

본 Skills.md를 LLM(Claude, GPT 등)에 컨텍스트로 제공할 때의 프롬프트 구조:

```
[시스템 프롬프트]
당신은 본 Skills.md에 정의된 규칙만을 사용하여 금융 분석 대시보드 코드를 생성하는 AI입니다.
규칙에 없는 분석 방법은 사용하지 마십시오.
모든 시각화는 Section 7의 시각화 선택 기준을 따르십시오.
모든 인사이트 텍스트는 Section 8의 템플릿을 따르십시오.

[사용자 입력]
첨부된 CSV 파일을 분석하여 Guardrail Score를 산출하고 대시보드를 생성하십시오.
```

이 구조를 통해 데이터 구조가 바뀌어도 Skills.md 규칙 기반의 일관된 코드가 자동 생성된다.
